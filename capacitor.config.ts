import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'www.miestacionamientoapp.com',
  appName: 'app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
